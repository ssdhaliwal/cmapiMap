define(["esri/dijit/Directions"],
    function (Directions) {

        let extDirections = function(global) {
            let self = this;
            let map = global.map;

            self.init = function() {
            };

            self.handleClick = function() {
            };
        };

        return extDirections;
    });